{
	"name": "base3system_entrytag",
	"label": "XRM Data Tag",
	"description": "Tag and keyword data records for XRM entries",
	"domain": "xrm",
	"category": "data",
	"tags": ["tag", "keyword", "label", "entry"],
	"fields": [
		{ "name": "id", "type": "integer", "label": "Tag ID", "required": true },
		{ "name": "tag", "type": "string", "label": "Tag name", "required": true }
	],
	"joins": [
		{
			"targetTable": "base3system_sysentry",
			"on": { "base3system_entrytag.id": "base3system_sysentry.id" },
			"type": "INNER",
			"meta": { "default": true }
		}
	],
	"defaultFilters": [],
	"position": { "x": -380, "y": -190 }
}
